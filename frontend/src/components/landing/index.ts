// src/components/landing 배럴
export * from "./ProblemCards";
// Originkit CLI로 설치된 워드글로브 히어로 — 파일은 Tailwind 스캔 경로 유지를 위해
// src/components/originkit에 그대로 두고 여기서만 재노출한다.
export { default as WordGlobeHeroSection } from "@/components/originkit/hero-09";
